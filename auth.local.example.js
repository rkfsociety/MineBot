'use strict'

/**
 * Скопируйте в auth.local.js и заполните. Файл auth.local.js в .gitignore.
 */
module.exports = {
  password: 'ВАШ_ПАРОЛЬ',
  /** Ник бота в Minecraft (если не задан — MC_USER или tester) */
  username: 'tester',
}
