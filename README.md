# MineBot

## Windows (релиз)
Важно: файл `MineBot.exe` **не запускается в одиночку** — ему нужны файлы рядом (`resources/`, dll и т.д.). Поэтому для запуска скачивайте **архив** релиза и распаковывайте целиком.